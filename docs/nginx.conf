server {
  listen 80;
  server_name localhost;
  absolute_redirect off;

  location /skills/ {
    alias /usr/share/nginx/html/skills/;
    try_files $uri $uri/ =404;
  }

  location = / {
    return 302 /skills/;
  }
}
